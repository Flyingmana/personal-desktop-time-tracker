# Tasks: Log an hour of work

> This is a template/example. Copy this folder's structure for real stories and delete this note.

- [x] Add `incrementCount(current: Int): Int` pure function
- [x] Add `App()` composable with counter state, tagged `Text` and `Button`
- [x] Unit test `incrementCount` (Kotest)
- [x] UI test: clicking the button updates the displayed count (Compose UI Test)
